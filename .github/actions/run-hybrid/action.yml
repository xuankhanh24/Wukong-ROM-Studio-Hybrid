name: Chạy Wukong hybrid recipe
description: Materialize one private recipe and run the shared Wukong orchestrator
inputs:
  recipe_ref:
    required: true
  recipe_b64:
    description: Validated secret-free recipe passed from the route job
    required: false
  job_id:
    required: true
  rclone_config_b64:
    required: true
  github_token:
    required: false
  control_plane_api_url:
    required: false
  actions_callback_secret:
    required: false
  github_run_id:
    required: false
  self_hosted:
    required: true
  dccloud_mirror_enabled:
    required: false
    default: "false"
  dccloud_remote:
    required: false
    default: "wukong-dccloud"
  dccloud_root:
    required: false
    default: "ROM"
  dccloud_share_url:
    required: false
    default: ""
  dccloud_webdav_url:
    required: false
    default: ""
  dccloud_upload_mode:
    required: false
    default: "multipart"
  dccloud_api_url:
    required: false
    default: ""
  dccloud_refresh_token:
    required: false
    default: ""
  dccloud_cloudreve_version:
    required: false
    default: ""
outputs:
  executor_started:
    description: Whether the ROM executor process was entered
    value: ${{ steps.execute.outputs.executor_started }}
runs:
  using: composite
  steps:
    - name: "A · Công cụ hệ thống / System tools"
      shell: bash
      run: |
        set -euo pipefail
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends cpio p7zip-full brotli zstd rclone e2fsprogs lz4 zip unzip
        python3 -m pip install --user -r requirements.txt
    - name: "B · Quyền truy cập Drive / Private Drive access"
      shell: bash
      env:
        RCLONE_CONFIG_B64: ${{ inputs.rclone_config_b64 }}
      run: |
        set -euo pipefail
        mkdir -p .wkstudio/secrets
        umask 077
        printf '%s' "$RCLONE_CONFIG_B64" | base64 --decode > .wkstudio/secrets/rclone.conf
    - name: "B1 · Preflight DC Cloud mirror"
      shell: bash
      env:
        WUKONG_DCCLOUD_MIRROR_ENABLED: ${{ inputs.dccloud_mirror_enabled }}
        WUKONG_DCCLOUD_REMOTE: ${{ inputs.dccloud_remote }}
        WUKONG_DCCLOUD_ROOT: ${{ inputs.dccloud_root }}
        WUKONG_DCCLOUD_SHARE_URL: ${{ inputs.dccloud_share_url }}
        WUKONG_DCCLOUD_WEBDAV_URL: ${{ inputs.dccloud_webdav_url }}
        WUKONG_DCCLOUD_UPLOAD_MODE: ${{ inputs.dccloud_upload_mode }}
        WUKONG_DCCLOUD_API_URL: ${{ inputs.dccloud_api_url }}
        WUKONG_DCCLOUD_REFRESH_TOKEN: ${{ inputs.dccloud_refresh_token }}
        WUKONG_DCCLOUD_CLOUDREVE_VERSION: ${{ inputs.dccloud_cloudreve_version }}
      run: |
        set -euo pipefail
        python3 -m tools.dccloud_preflight --config .wkstudio/secrets/rclone.conf --write-test
    - name: "B2 · Thư mục job dùng chung / Shared job store"
      shell: bash
      run: |
        printf '%s\n' 'WUKONG_ACTIONS_JOBS_ROOT=.wkstudio/Jobs/hybrid' >> "$GITHUB_ENV"
    - name: "C · Recipe & content-pack / Fetch private build content"
      shell: bash
      env:
        RECIPE_REF: ${{ inputs.recipe_ref }}
        RECIPE_B64: ${{ inputs.recipe_b64 }}
        WUKONG_RCLONE_CONFIG: ${{ github.workspace }}/.wkstudio/secrets/rclone.conf
      run: |
        set -euo pipefail
        if [ -n "$RECIPE_B64" ]; then
          printf '%s' "$RECIPE_B64" | base64 --decode > .wkstudio/recipe.json
        else
          python3 -m tools.fetch_recipe "$RECIPE_REF" --output .wkstudio/recipe.json --rclone-config "$WUKONG_RCLONE_CONFIG" --coerce-github
        fi
        task="$(python3 -c "import json; print(json.load(open('.wkstudio/recipe.json'))['task'])")"
        if [ "$task" = build ]; then
          mod_version="$(python3 -c "import json; print(json.load(open('.wkstudio/recipe.json'))['build']['modVersion'])")"
          mkdir -p .wkstudio/bundled-shared
          rm -rf .wkstudio/bundled-shared/Fake_lock
          cp -a STARK/Fake_lock .wkstudio/bundled-shared/Fake_lock
          install_pack() {
            local pack="$1"
            if python3 content_pack_tool.py verify --content-root . --index content-packs/index.json --pack "$pack" >/dev/null 2>&1; then
              echo "[Wukong] Content cache hit: $pack"
            else
              echo "[Wukong] Content cache miss: $pack"
              python3 content_pack_tool.py install --content-root . --index content-packs/index.json --pack "$pack" --rclone-config "$WUKONG_RCLONE_CONFIG"
            fi
          }
          install_pack "MOD/$mod_version"
          for pack in STARK/common Flash_script/common copy-image/v1 OFX/v1 TWRP/v1; do
            install_pack "$pack"
          done
          # The current remote STARK archive predates the shared Fake_lock.
          # Restore its bundled canonical tree after the atomic pack install.
          rm -rf STARK/Fake_lock
          cp -a .wkstudio/bundled-shared/Fake_lock STARK/Fake_lock
          # Validate before downloading the multi-GB ROM. Strict mode preserves
          # channel parity: a requested MOD is never silently removed.
          if [ "${WUKONG_DROP_MISSING_MODS:-0}" = 1 ]; then
            python3 -m tools.validate_recipe_content --recipe .wkstudio/recipe.json --content-root . --drop-missing --rewrite
          else
            python3 -m tools.validate_recipe_content --recipe .wkstudio/recipe.json --content-root . --strict
          fi
        fi
        if [ '${{ inputs.self_hosted }}' != true ]; then
          python3 -m tools.runner_preflight --recipe .wkstudio/recipe.json --reserve-gib 4 --min-disk-gib 4 --min-memory-gib 0 --min-cpus 0
        fi
    - name: "C2 · Nhận diện ROM nguồn / Identify source ROM"
      shell: bash
      run: |
        set -euo pipefail
        source_uri="$(python3 -c "import json; print(json.load(open('.wkstudio/recipe.json'))['source']['uri'])")"
        case "$source_uri" in
          http://*|https://*)
            if python3 wukong_cli.py probe-source "$source_uri" > .wkstudio/source-probe.json; then
              python3 -m tools.merge_source_probe --recipe .wkstudio/recipe.json --probe .wkstudio/source-probe.json
              python3 - <<'PY'
        import json, os
        probe = json.load(open('.wkstudio/source-probe.json', encoding='utf-8'))
        size = probe.get('sizeBytes')
        def safe(value):
            return str(value or '—').replace('`', "'").replace('\r', ' ').replace('\n', ' ')[:240]
        rows = [
            '## C2 · Nhận diện ROM nguồn / Source ROM identity',
            '',
            f"- Provider: `{safe(probe.get('provider'))}`",
            f"- File: `{safe(probe.get('filename'))}`",
            f"- Product: `{safe(probe.get('productName'))}`",
            f"- Device: `{safe(probe.get('device'))}`",
            f"- Version: `{safe(probe.get('version'))}`",
            f"- Security patch: `{safe(probe.get('securityPatch'))}`",
            f"- OTA: `{safe(probe.get('otaType'))}`",
            f"- Size: `{size if size else '—'}` bytes",
        ]
        with open(os.environ['GITHUB_STEP_SUMMARY'], 'a', encoding='utf-8') as summary:
            summary.write('\n'.join(rows) + '\n')
        PY
            else
              echo "::warning title=C2 · Source ROM identity::Metadata probe unavailable; the normal downloader will retry during stage 05."
              printf '%s\n' '## C2 · Nhận diện ROM nguồn / Source ROM identity' '' '> Metadata probe unavailable. The normal downloader will retry during stage 05.' >> "$GITHUB_STEP_SUMMARY"
            fi
            ;;
        esac
    - name: "D · Toolchain Linux / Bootstrap & smoke test"
      shell: bash
      run: |
        set -euo pipefail
        python3 -m tools.bootstrap_linux
        python3 -m tools.smoke_linux
    - name: "E · Pipeline 05–19 / Execute staged ROM build"
      id: execute
      shell: bash
      env:
        WUKONG_JOB_ID: ${{ inputs.job_id }}
        WUKONG_RCLONE_CONFIG: ${{ github.workspace }}/.wkstudio/secrets/rclone.conf
        WUKONG_GITHUB_REPOSITORY: ${{ github.repository }}
        WUKONG_GITHUB_TOKEN: ${{ inputs.github_token }}
        WUKONG_CONTROL_PLANE_API_URL: ${{ inputs.control_plane_api_url }}
        WUKONG_ACTIONS_CALLBACK_SECRET: ${{ inputs.actions_callback_secret }}
        WUKONG_RUN_ID: ${{ inputs.github_run_id }}
        # Terminal reporting is owned by the always-on control plane after the
        # public artifact URL has been synchronized. Avoid an early duplicate
        # notification from the ephemeral Actions runner.
        WUKONG_TELEGRAM_BOT_TOKEN: ""
        WUKONG_TELEGRAM_CHAT_ID: ""
        WUKONG_DROP_MISSING_MODS: "0"
        WUKONG_DCCLOUD_MIRROR_ENABLED: ${{ inputs.dccloud_mirror_enabled }}
        WUKONG_DCCLOUD_REMOTE: ${{ inputs.dccloud_remote }}
        WUKONG_DCCLOUD_ROOT: ${{ inputs.dccloud_root }}
        WUKONG_DCCLOUD_SHARE_URL: ${{ inputs.dccloud_share_url }}
        WUKONG_DCCLOUD_WEBDAV_URL: ${{ inputs.dccloud_webdav_url }}
        WUKONG_DCCLOUD_UPLOAD_MODE: ${{ inputs.dccloud_upload_mode }}
        WUKONG_DCCLOUD_API_URL: ${{ inputs.dccloud_api_url }}
        WUKONG_DCCLOUD_REFRESH_TOKEN: ${{ inputs.dccloud_refresh_token }}
        WUKONG_DCCLOUD_CLOUDREVE_VERSION: ${{ inputs.dccloud_cloudreve_version }}
      run: |
        set -euo pipefail
        job_json="$(python3 wukong_cli.py --jobs-root "$WUKONG_ACTIONS_JOBS_ROOT" submit --recipe .wkstudio/recipe.json --job-id "$WUKONG_JOB_ID" --channel actions --subject "$GITHUB_ACTOR_ID" --role user)"
        job_id="$(printf '%s' "$job_json" | python3 -c 'import json,sys; print(json.load(sys.stdin)["job_id"])')"
        # Best-effort resume only; a missing/expired checkpoint must not fail the step.
        python3 -m tools.sync_job "$job_id" --config "$WUKONG_RCLONE_CONFIG" --jobs-root "$WUKONG_ACTIONS_JOBS_ROOT" --pull-checkpoint || true
        reporter_pid=""
        if [[ -n "$WUKONG_CONTROL_PLANE_API_URL" && -n "$WUKONG_ACTIONS_CALLBACK_SECRET" && -n "$WUKONG_RUN_ID" ]]; then
          python3 -m tools.actions_control_plane progress \
            --api-url "$WUKONG_CONTROL_PLANE_API_URL" \
            --job-id "$job_id" \
            --run-id "$WUKONG_RUN_ID" \
            --secret "$WUKONG_ACTIONS_CALLBACK_SECRET" \
            --jobs-root "$WUKONG_ACTIONS_JOBS_ROOT" \
            --sequence-offset 2 &
          reporter_pid="$!"
        fi
        set +e
        echo "executor_started=true" >> "$GITHUB_OUTPUT"
        python3 wukong_cli.py --jobs-root "$WUKONG_ACTIONS_JOBS_ROOT" execute "$job_id"
        result=$?
        set -e
        if [[ -n "$reporter_pid" ]]; then
          for _ in {1..30}; do
            kill -0 "$reporter_pid" 2>/dev/null || break
            sleep 1
          done
          kill "$reporter_pid" 2>/dev/null || true
          wait "$reporter_pid" 2>/dev/null || true
        fi
        echo "result=$result" >> "$GITHUB_OUTPUT"
    - name: "F · Đồng bộ manifest & log / Sync result to Drive"
      if: always()
      shell: bash
      env:
        WUKONG_JOB_ID: ${{ inputs.job_id }}
        WUKONG_RCLONE_CONFIG: ${{ github.workspace }}/.wkstudio/secrets/rclone.conf
      run: |
        set +e
        python3 -m tools.sync_job "$WUKONG_JOB_ID" --config "$WUKONG_RCLONE_CONFIG" --jobs-root "$WUKONG_ACTIONS_JOBS_ROOT"
        # Pre-execute failures have no local job store yet; do not fail the workflow twice.
        exit 0
    - name: "F1 · Kiểm tra quota DC Cloud / Check mirror quota"
      if: always()
      continue-on-error: true
      shell: bash
      env:
        WUKONG_DCCLOUD_MIRROR_ENABLED: ${{ inputs.dccloud_mirror_enabled }}
        WUKONG_DCCLOUD_REMOTE: ${{ inputs.dccloud_remote }}
        WUKONG_DCCLOUD_ROOT: ${{ inputs.dccloud_root }}
        WUKONG_DCCLOUD_SHARE_URL: ${{ inputs.dccloud_share_url }}
        WUKONG_DCCLOUD_UPLOAD_MODE: ${{ inputs.dccloud_upload_mode }}
        WUKONG_DCCLOUD_API_URL: ${{ inputs.dccloud_api_url }}
        WUKONG_DCCLOUD_REFRESH_TOKEN: ${{ inputs.dccloud_refresh_token }}
        WUKONG_DCCLOUD_CLOUDREVE_VERSION: ${{ inputs.dccloud_cloudreve_version }}
        WUKONG_RCLONE_CONFIG: ${{ github.workspace }}/.wkstudio/secrets/rclone.conf
      run: python3 -m tools.dccloud_quota
    - name: "G · Xác nhận kết quả / Enforce terminal status"
      if: always() && steps.execute.outputs.result != '0'
      shell: bash
      run: exit 1
